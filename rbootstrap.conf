# This is just a configuration file for performing tests during development.
# You can use the configuration file to completely configure the operation
# mode of rbootstrap. Alternatively, if you do not like to use a configuration
# file, you can control the behaviour of rbootstrap by it's command line
# options. Take a look at "rboostrap --help" or "man rbootstrap" for details.

codename    = 'opensuse13.1'
arch        = 'amd64'
distro_path = 'distros'
root        = 'chroot'
